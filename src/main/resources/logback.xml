<configuration>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- To suppress debug logs from Jetty -->
    <logger name="org.eclipse.jetty" level="WARN"/>
</configuration>
